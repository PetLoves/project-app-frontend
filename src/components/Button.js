import React from 'react';
import { Button } from './components/Button.jsx'

function click() {
    {this.props.animal};
}

const myButton = (
    <button onClick={click}>Here's more details</button>
)

class Button extends React.Component {
    render (
        click,
        myButton
    )
  }

export default Button;
